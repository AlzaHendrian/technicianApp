import { authenticationState } from "./authenticationState";
import { teknisiState } from "./teknisiState";

export const initialState = {
    authentication: authenticationState,
    teknisi: teknisiState
}